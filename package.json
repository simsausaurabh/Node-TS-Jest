{
  "dependencies": {
    "@types/jest": "^24.0.11",
    "express": "^4.16.4",
    "jest-cli": "^24.5.0",
    "jest-html-reporters": "^1.1.3",
    "ts-jest": "^24.0.0",
    "typescript": "^3.3.3333"
  },
  "s": "tsc && mocha dist/**/*.spec.js",
  "tsc": "tsc",
  "devDependencies": {
    "jest": "^24.5.0"
  },
  "scripts": {
    "test": "jest --config ./config/server.config.js",
    "cov": "jest --coverage test --config ./config/server.config.js --no-cache"
  }
}
